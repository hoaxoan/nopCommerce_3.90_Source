@model IList<CategoryModel>
@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        @Html.Widget("home_page_before_products")
        @Html.Action("HomepageProducts1", "Product", new { categoryId = item.Id })
    }
}